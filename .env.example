# ═══════════════════════════════════════════════════════════════
# Voice Avatar Frontend - Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values
# Vite requires VITE_ prefix for env vars to be exposed to client

# ── API Configuration ──
# Development: leave empty (uses Vite proxy)
# Production: leave empty (Caddy proxies /api/* to backend)
VITE_API_URL=
